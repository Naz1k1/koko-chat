# 复制为本目录的 .env 后填写；.env 不提交 Git，权限应为 600。
# 直接填写值，不使用 shell 语法；特殊字符无需引号或转义。
# 支持 STARTTLS（通常 587）；465 使用隐式 TLS。
SMTP_SMARTHOST=
SMTP_FROM=
SMTP_TO=
SMTP_USERNAME=
SMTP_PASSWORD=
# 可选：逗号分隔的宿主机后端地址，默认 host.docker.internal:8080。
KOKO_MONITOR_TARGETS=host.docker.internal:8080
